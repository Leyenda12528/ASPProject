
@{
    ViewBag.Title = "Categorias";
}

<div id="app" v-cloak>
    <div class="d-flex justify-content-between my-3">
        <h2>Categorias</h2>
        <button class="btn btn-primary" data-toggle="modal" data-target="#ModalC" v-on:click="modal(1)">
            <img src="~/Imagenes/icons/plus-black.png" width="30" height="30" /> Agregar
        </button>
    </div>

    <div class="d-flex justify-content-center">
        <table class="table table-striped">
            <thead class="thead-dark">
                <tr>
                    <th v-for="dato in Encabezados" scope="col">{{dato}}</th>
                </tr>
            </thead>
            <tbody>
                <tr v-for="dato in Datos" v-on:click="modal(2, dato)" data-toggle="modal" data-target="#ModalC">
                    <th scope="row">{{dato.ID}}</th>
                    <td>{{dato.Name}}</td>
                </tr>
            </tbody>
        </table>
    </div>

    <!-- Modal -->
    <div class="modal fade" id="ModalC" tabindex="-1" role="dialog" aria-labelledby="ModalCTitle" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered" role="document">
            <div class="modal-content">
                <div class="modal-header bg-primary">
                    <h5 class="modal-title text-white">{{Modal.Titulo}}</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <div class="col-md-12 p-0">
                        <label class="font-weight-bold">{{Modal.Registro.text}}</label>
                        <input type="text" class="form-control rounded-pill" v-model="Modal.Registro.Nombre"
                               v-bind:class="{'is-invalid' : Modal.Registro.invalido}"
                               :placeholder="Modal.Registro.placeholder"/>
                        <div class="invalid-feedback">
                            {{Modal.Registro.error}}
                        </div>
                    </div>
                    <div class="col-md-12 p-0">
                        <button type="button" class="btn btn-block btn-primary2 rounded-pill font-weight-bold mt-3" v-on:click="agregar"
                                :disabled="Modal.Boton.disabled">
                            {{Modal.Boton.texto}}
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@Scripts.Render("~/Scripts/v4/Categoria/Categoria.js")